<!-- Example: a real run of `python -m cu_openalex.eval` against the live site. Regenerate per release. -->

> Generated by the eval harness against https://insights.uccc.cancerdatasci.org.

# Eval scorecard

## Content (NL->SQL chat)

- **read_only**: 7/7 (100%)
- **schema_valid**: 5/5 (100%)
- **transparent**: 5/5 (100%)
- **refusal_ok**: 2/2 (100%)

## Backend / capability coverage

- `pass` **health** — ok
- `pass` **meta.current_programs=4** — 4 current programs
- `pass` **kpi.publications>0** — nonzero
- `pass` **program-summary=4** — 4 programs with collaboration %
- `pass` **top-topics rejects bad input (422)** — HTTP 422
- `partial` **R7 field-normalized impact (FWCI + %top-1/10)** — FWCI present; top-percentile share not yet exposed
- `absent` **R3 fiscal-year windows** — meta exposes calendar-year window only
- `absent` **R1/R2 provenance + reportable mode**
- `present` **R12 editable profiles (backend)** — profile read endpoint reachable
- `present` **Membership spine read-API**
- `absent` **R8 translational metrics (iCite APT/clinical)**

_Content + capability layers are runnable now; UI/UX heuristic and persona task-walkthrough layers (Playwright) are iterations 2-3 (docs/eval/05). Agent scores are directional/regression signals, not the sole gate._